/Controllers - LoginController.cs - FeedbackController.cs - AdminController.cs

/Models - Student.cs - Faculty.cs - Course.cs - FeedbackResponse.cs

/Views /Student - FeedbackForm.cshtml - Dashboard.cshtml /Admin - Analytics.cshtml - ManageUsers.cshtml

/Database - schema.sql

1. Clone the repo
2. Open the project in Visual Studio
3. Restore/verify NuGet packages
4. Import the SQL file into SQL Server (if included)
5. Update the connection string in appsettings.json or Web.config depending on your framework version
6. Run the project ✔️ The app runs on https://localhost:/
